# Agent Context

This project is a neovim config written in lua. It is designed to be modular and easily customizable.

## Commit Style

- Single-line, no body: `feat(nvim): <summary>`
- Scope is always `nvim`
- Prefix: `feat` for features, `fix` for bug fixes
