{
    "max function": {
        "prefix": "max",
        "body": [
            "func max(x int, others ...int) int {",
            "   res := x",
            "   for _, y := range others {",
            "       if y > res {",
            "           res = y",
            "       }",
            "   }",
            "   return res",
            "}"
        ],
        "description": "Snippet for a max function"
    },
    "min function": {
        "prefix": "min",
        "body": [
            "func min(x int, others ...int) int {",
            "   res := x",
            "   for _, y := range others {",
            "       if y < res {",
            "           res = y",
            "       }",
            "   }",
            "   return res",
            "}"
        ],
        "description": "Snippet for a min function"
    }
}
