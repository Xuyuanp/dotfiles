- defaults:
    link:
      relink: true

- clean: ['~']

- shell:
  - git config --global include.path ${PWD}/gitconfig

- link:
    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zshrc
    ~/.config/nvim: nvim
